/** Placeholder until the schema rewrite lands. */
export default function ListingsRoute() {
  return (
    <div className="mx-auto max-w-6xl px-4 py-10">
      <h1 className="text-3xl font-semibold">Listings</h1>
      <p className="mt-3 text-muted-foreground">
        Listings appear here once the property schema is in place.
      </p>
    </div>
  );
}
